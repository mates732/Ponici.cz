# Project conventions

- This project uses TanStack Start with Vite.
- Route files go in `src/routes/`, following file-based routing conventions.
- All components, hooks, and utilities are in `src/`.
- Styling is done with Tailwind CSS v4.
- Never commit generated files like `routeTree.gen.ts` or `package-lock.json` unless they are tracked.
